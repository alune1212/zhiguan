# 值观路线图

**状态：** 开发优先的阶段记录，不承诺日期或功能数量。

## Now：把单次购买闭环做得可用

当前目标是让用户用少量输入完成：

~~~text
一句话描述 → 按需回答一个必要问题或点快捷选项 → 编辑待确认摘要并整体确认一次 → 查看核心结果 → 主动开启余量 → 记录自己的决定
~~~

对话优先输入已获批准，正在实施：一句话可整理收入、税口径、价格和工作时间信息；代码按缺项选择固定追问，用户可不知道、跳过或改用手动填写。两种入口共用草稿，只需整体确认一次。余量仅在用户主动开启后追问。修改确认后的输入会使旧结果和导出失效，并清除旧决定及理由。服务失败保留回答与草稿，用户可主动重试或接续手动填写。

优先处理真实用户功能、错误修正、可解释结果和低负担交互。工作时间可通过平时作息或次级的直接月工时入口提供，作息换算结果保持 estimated。先展示工作时间结论，以每小时收入作为补充。只有用户主动开启余量后才显示余量输入并追问固定支出等内容；缺少完整覆盖或只提供税前收入时只让已开启的余量结果显示 insufficient-data，不阻塞核心结果；购买后余量还需确认按本月付款。页面打开、手动输入及快捷按钮不调用 Jev。用户主动发送对话时，首次发送前说明处理方和范围；本机服务仅转发当前回答、当前问题与理解所需的最少相关字段或原文片段，不发送完整表单、全部对话、价值期待或决定理由。会话保持浏览器内存态，不持久化；JSON 导出不包含对话原文。

独立的 **Goal 进度**曾作为最小切片加入，但已于 2026-09-01 按 PRD-0002 回滚条件撤下：用户确认它增加输入负担且未帮助购买任务。PRD-0002 保留为 Retired 历史，不代表当前实现。

研究招募、研究记录、存储与删除演练暂停，不是 Now 的开发前置条件。Jev 能力限于本机回环 Python 服务承接的单次购买对话识别；密钥由服务端管理，不记录请求正文。用户可主动重试，系统不自动重试；识别或服务失败时保留进度并允许手动接续。

## Next：只在当前闭环需要时扩展

订阅、物品使用、目标历史/预测、导入等能力暂不承诺。购买工作台交付后，下一步先收集实际使用反馈，不自动追加新功能；反馈不构成研究或市场结论。

## Later：明确不进入当前开发

账户、跨设备同步、云端服务、协作分享、AI 建议、自由文本生成、完整账本和自动提醒均不进入当前实现；当前唯一的 AI 例外是本路线图前文规定的单次购买对话输入。其他扩展需要新的产品决定以及适用的 ADR。

## 排序规则

能直接改善单次购买闭环的用户功能优先；只增加治理、报告或未来扩展结构而不改善当前使用的工作后置或删除。路线图不替用户需求制造确定性承诺。
