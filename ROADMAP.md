# 值观路线图

**状态：** 开发优先的阶段记录，不承诺日期或功能数量。

## Now：把单次购买闭环做得可用

当前目标是让用户用少量输入完成：

~~~text
输入 → 确认口径 → 理解五个结果 → 记录自己的决定
~~~

优先处理真实用户功能、错误修正、可解释结果和低负担交互。保持浏览器内存态、无网络/持久化和用户主动 JSON 导出。

当前闭环稳定后，Now 中下一项产品功能是一个最小的 **Goal 进度**：只接受一个用户定义的目标基准和当前进度，展示已完成部分、剩余差距与证据状态。先不做历史、提醒、预测、同步或排名。

研究招募、研究记录、存储与删除演练暂停，不是 Now 的开发前置条件。

## Next：只在当前闭环需要时扩展

订阅、物品使用、目标历史/预测、导入等能力暂不承诺。只有当前闭环的实际使用反馈表明新增能力能减少重复输入或改善决定，才逐项讨论。

## Later：明确不进入当前开发

账户、跨设备同步、云端服务、协作分享、AI、完整账本和自动提醒均不进入当前实现。它们需要新的产品决定以及适用的 ADR。

## 排序规则

能直接改善单次购买闭环的用户功能优先；只增加治理、报告或未来扩展结构而不改善当前使用的工作后置或删除。路线图不替用户需求制造确定性承诺。
