# 值观协作规则

这是仓库唯一的 AI/贡献者执行入口。产品方向以 `PRODUCT.md` 为最高约束。

## 开始任务

先阅读：

1. `PRODUCT.md`
2. `PRODUCT_STRATEGY.md`
3. `ROADMAP.md`
4. `ARCHITECTURE.md`
5. 与任务直接相关的 PRD、ADR 或其他设计文档

产品宪法优先于策略、路线图、PRD、ADR 和实现。发现冲突先停下并说明，不自行改写产品边界。

## 执行原则

- 实际用户功能优先；保持改动小、直接、可回滚。
- 不要求先补流程材料或证据包才能编码。只有问题确实需要时才补文档。
- 只有不可逆的数据/隐私决策、外部服务接入、持久化、同步或公开格式变更才先写 ADR。
- 保留输入校验、`actual` / `user-confirmed` / `estimated` / `forecast` / `insufficient-data` 状态、隐私最小化、可解释性和可访问性。
- 不加入账户、排名、人生总分、强制提醒、羞辱性文案或未经证据支持的确定性建议。
- 不把真实个人财务数据、密钥、未脱敏日志或截图写入仓库。

## 验证与交付

- 运行与改动相关的测试、类型检查或构建；无法运行时说明原因。
- 缺失、冲突或失败数据必须诚实降级，不能用零值、旧值或猜测填补。
- 说明改动范围、验证结果和剩余不确定性；不要顺手重构无关代码。
