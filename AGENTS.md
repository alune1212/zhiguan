# 值观协作与代理规则

本文件是仓库中 AI 代理和贡献者的执行入口。它约束产品设计、文档、代码、数据和发布工作；产品方向以根目录 `PRODUCT.md` 为准，不能用本文件替代产品宪法。

## 开始任何任务前

在提出方案、修改文档、设计数据、写代码或调整配置前，必须先阅读：

1. `PRODUCT.md`：产品宪法、长期边界和不可轻易改变的原则。
2. `PRODUCT_STRATEGY.md`：当前阶段、目标用户和验证中的策略假设。
3. `ROADMAP.md`：当前优先级、阶段出口和暂不承诺的方向。
4. `ARCHITECTURE.md`：隐私、数据主权、可解释性、历史口径和可迁移性等架构约束。
5. 与任务直接相关的 `docs/product/`、`docs/rfcs/`、`docs/adr/`、PRD、RFC 和 ADR。

文档优先级固定为：

PRODUCT.md > PRODUCT_STRATEGY.md > ROADMAP.md > PRD/RFC > Issue > 实现

`ARCHITECTURE.md` 服从 `PRODUCT.md`，并约束 PRD、RFC、ADR、Issue 与实现。批准后的 ADR 在适用技术范围内优先于 Issue 和实现，但不能覆盖上位产品文档。

如果必需文档尚不存在，先补齐文档或提出 RFC；不得把缺失的产品决策自行当作已批准的需求。

## 每项设计和实现的产品门禁

提交 Issue、PRD、RFC、ADR、代码或配置前，必须能在说明中回答以下问题：

- **产品对齐**：服务哪个用户、哪个具体任务或痛点，属于价值循环的哪个阶段（Capture、Normalize、Understand、Simulate、Decide、Review、Learn），预期带来什么可观察结果，依据是什么。
- **数据与信任**：每个关键数字标明来源、时间和口径，并明确属于实际（actual）、用户确认（user-confirmed）、估算（estimated）、预测（forecast）或数据不足（insufficient-data）；不得把估算或预测伪装成事实。
- **隐私与数据主权**：说明收集什么、为什么收集、存在哪里、谁能访问、保存多久，以及导出、更正和删除方式；默认最小化收集，不把真实收入、资产、订阅或身份数据写入示例、测试、日志或截图。
- **可解释性**：用户能看到关键输入、公式或推导、来源、时间戳、假设、不确定性和修正路径；结果不应是无法申诉的黑箱分数。
- **输入负担**：说明首次设置和持续维护需要用户付出什么，是否能复用已有数据、批量处理或自动化，以及数据缺失时如何诚实降级。
- **反焦虑**：文案和交互不得羞辱、恐吓、催促或制造财富排名、人生总分、连续打卡压力；提供安静默认值、关闭提醒和用户控制，并承认非货币化生活价值。
- **范围与出口**：明确本次包含什么、不包含什么、成功标准、退出或回滚条件，以及后续判断需要哪些证据。

上述内容可以分布在 PRD、RFC、ADR 和 Issue 中，但 PR 必须给出链接和摘要。没有完成产品门禁的工作只能停留在探索或文档阶段，不能直接进入实现。

## 决策与变更规则

- 与 `PRODUCT.md` 的原则、产品边界、核心用户承诺或价值循环定义冲突时，必须先提交 RFC，写清冲突、备选方案、影响、迁移和回滚；RFC 批准并更新版本/影响记录后才能实现。
- 涉及数据模型、历史口径、隐私边界、同步、外部服务、可解释性或不可逆迁移时，必须先有 ADR；技术栈在架构 ADR 决定前不得被实现细节锁定。
- 新功能和实验必须从 GitHub Issue Form 开始；Bug 必须提供影响、数据/隐私影响、复现步骤和回归信息。PR 不得绕过这些门禁直接以“顺手改动”进入主干。
- 外部 Bug 报告可以只完成 Bug 表单的最低字段，避免让报告者承担产品设计工作；开始修复前，实现者仍必须在修复 PR 或相关 PRD/ADR 中补齐适用的产品、数据、解释、输入负担、范围和退出门禁。
- 保持改动小而可回滚。不要为了完成当前任务顺手重构无关模块、改变已有数据口径或引入未经批准的依赖。
- 初始化阶段仅维护治理文档和协作入口；应用框架、运行时 API、数据库 Schema、部署和 CI 需要后续明确的 PRD/ADR。

## 实现和审查要求

- 代码、配置和文档中的产品术语遵循产品术语表；指标名称、单位、币种、时区、时间范围和数据状态必须明确。
- 任何面向用户的数字都应能追溯到输入和计算过程；无法验证时显示“不足以判断”或等价状态，而不是填充看似精确的数字。
- 测试覆盖正常路径、空数据、过期数据、冲突输入、权限边界、导出/删除和失败降级；涉及界面时同时检查移动端、可访问性和低刺激文案。
- 不提交密钥、个人数据、真实账单、真实工资、未脱敏截图或用户可识别日志。调试输出同样遵守最小化原则。
- PR 描述必须完成模板中的产品对齐、数据与信任、范围、成功/退出、隐私、可解释性、输入负担、反焦虑和验证清单；审查者应以证据而非实现规模判断是否可合并。
- 默认使用功能分支和 PR；主分支只接收经过审查、可验证、可回滚的改动。

## 不确定时的处理

先标注“不确定”“待复核”或“数据不足”，并记录需要验证的事实、责任人和截止时间。不要通过猜测填补产品策略、数据口径或用户意图；如果判断可能改变宪法或长期方向，升级为 RFC。
