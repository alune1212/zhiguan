# OpenSpec apply 实施基线

| 项目 | Live readback |
| --- | --- |
| 日期 | `2026-08-24`（Asia/Shanghai） |
| OpenSpec change | `build-single-purchase-decision-workbench` |
| OpenSpec 状态 | `spec-driven` / `ready`；批准记录为 `Approved` |
| PRD 门禁 | `Implementation Authorized` 已通过 |
| 架构门禁 | `ADR-0001`、`ADR-0002`、`ADR-0003` 均为 `Accepted` |
| 实施前 commit | `3c0af839da21c95cd35667c87cfee50f2dd00343` |
| 分支 | `codex/prd-single-purchase-decision` |
| 实施前工作树 | clean；`git diff --check` 通过 |
| 未通过门禁 | `Prototype Accepted`、`Recruitment Authorized` |
| 研究协议 | `RESEARCH-0001` 仍为 `Draft` |

本 readback 仅证明 task 1.1 的实施授权前置条件成立，不是运行时、原型验收、招募或参与者分发证据。后续实现只允许覆盖已批准的 38 项任务；任何范围、公式、导出合同、隐私边界或交付拓扑变化均须先回到适用治理流程。

## 核验命令

```text
git status --short
git branch --show-current
git rev-parse HEAD
git diff --check
openspec status --change build-single-purchase-decision-workbench --json
openspec instructions apply --change build-single-purchase-decision-workbench --json
openspec validate build-single-purchase-decision-workbench --strict --no-interactive
```

核验输出未包含用户输入、研究编号、参与者标识或任何真实/近似个人数据。
