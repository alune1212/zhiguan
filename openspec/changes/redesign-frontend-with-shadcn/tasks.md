# Tasks

## 1. 概念稿与组件基础

- [x] 1.1 根据已确认页面方案交付手机和桌面概念稿，覆盖首次设置、正常收入首页、设置展开、数据不足与未保存状态；使用标注的合成数据，对照 DESIGN.md 核对主次层级与操作位置，记录审阅结论后再锁定页面细节。
- [x] 1.2 用 Bun 接入 Tailwind v4、Radix 基底的 shadcn/ui 和实际需要的图标组件，将主题合并到 `src/styles/app.css` 并配置一致的 Vite / TypeScript alias 与 `components.json`；检查 diff 保留代理和安全头，通过 `bun run check:dependencies` 与 `bun run typecheck`。
- [x] 1.3 用实际要使用的折叠、选择和确认控件验证构建产物在 preview 与 Docker 静态服务下可用；检查样式、焦点、资源与 CSP 控制台错误，修正组件兼容问题后再扩大迁移，不放宽现有 CSP。

## 2. 现有页面迁移

- [x] 2.1 迁移首次设置的字段、错误和保存反馈，保留一个必填收入、可见默认作息、折叠细节及恢复入口；更新 `tests/income-app.test.tsx` 的旧结构断言，并验证非法金额不能开始、有效输入不触发 Jev。
- [x] 2.2 迁移收入首页与页面内设置：主金额居中、辅助指标成组、顶栏收藏与设置、数字下方依据与购买；验证设置按钮展开定位及焦点、取消不保存、保存才重算，以及工作、休息、有效零和数据不足的不同呈现。
- [x] 2.3 迁移购买输入和结果，默认一句话且手动入口随时可达；更新 `tests/app.smoke.test.tsx` 对旧默认入口的断言，验证两种入口共用草稿、保留约数证据、主动发送、失败接续和工作时间主结果。
- [x] 2.4 迁移收藏为纵向快照列表，开放零收藏入口并提供返回及开始购买路径；验证空列表、无有效收入资料时进入基础设置、删除最后一项后的空态，以及详情和重新试算不改写历史快照。
- [x] 2.5 将保存并重算、覆盖恢复、清空、单项删除和重新载入并放弃编辑五类确认接入 AlertDialog，保留准确影响范围、默认取消、版本检查与失败保护；留下可重复的交互检查，证明取消、确认期间资料更新及操作失效均不会写入陈旧内容或丢失编辑，原有备份与删除测试保持通过。

## 3. 回归与交付

- [x] 3.1 完成页面迁移后清理无消费者的旧 CSS 和重复样式，核对所有实际控件使用统一 tokens / variants；运行 `bun run check`，确保 peer 依赖、类型、现有测试和构建全部通过。
- [x] 3.2 在 preview 完成首次设置 → 首页 → 设置 → 购买 → 收藏及备份恢复流程；用合成数据验证存储不可用、冲突、损坏和恢复取消，确认页面进入、计时与手动路径不发起 Jev 请求，并记录实际操作结果。
- [x] 3.3 在 320 / 375 / 414 / 768px 和桌面检查长金额、长中文、200% 缩放、对比度、键盘、弹层焦点及减少动态；复查 Docker 构建后的资源与核心交互，无内容裁切或逐秒读屏打断，记录已测环境和未覆盖项。
- [x] 3.4 按实际结果更新 DESIGN.md、页面方案和购买页面文案中的实现状态与入口说明；检查文档与最终 UI 一致，明确概念稿、自动检查、浏览器验收和仍待真实用户验证的边界。
