# Tasks

## 1. 主金额与动效边界

- [ ] 1.1 将 `@number-flow/react` 加入 `package.json` 和 Bun 锁文件，仅在今日主金额使用；运行 `bun run check:dependencies` 确认依赖可安装且无多余包。
- [ ] 1.2 为领域层两位小数字符串接入金额展示适配：仅在分位往返无损且浏览器支持时传给滚动组件，其余用原有完整文本；用大金额及 `9.99 → 10.00` 的合成值检查不丢分、不裁切。
- [ ] 1.3 只在同资料、同日期、相邻工作时段的真实递增分值上启用短距离向上滚动；初次进入、后台恢复、跨日、资料重算、回拨和不足恢复直接就位；用这些分支的定向检查确认没有补播或自行加钱。

## 2. 看板展示与控制

- [ ] 2.1 把现有 `rateDisplay` 的每秒速率移到主金额附近，保持“按设定作息估算，非实际到账”、本月静态金额及桌面/窄屏层级；在 320px 与桌面查看金额、单位和按钮无裁切或横向滚动。
- [ ] 2.2 添加当前页面内的暂停快照、暂停时刻与恢复控制；依据变化或 `insufficient-data` 时废弃旧快照；用受控时钟检查暂停冻结、恢复跳到当前、重开页面不沿用旧值，并核对“暂停展示”与“暂停增长”文案不同。
- [ ] 2.3 接入系统减少动态与辅助技术路径：关闭数位滚动但保留数值更新和暂停控制，完整金额只读一次、不会逐秒播报；通过减少动态、键盘和读屏的实际浏览器检查。

## 3. 验证与规格对齐

- [ ] 3.1 运行覆盖进位/千位分隔、低速不足一分、异常跳变、暂停期间资料失效及动效精度降级的定向 Bun 测试，确认全部通过且领域收入公式、证据状态与已有购买结果未改变。
- [ ] 3.2 将暂停展示和主金额旁速率的行为同步到 `docs/product/prd/PRD-0003-income-dashboard.md`，并核对文案与本变更 delta 一致；验证相关本地链接及 `git diff --check`。
- [ ] 3.3 运行 `bun run check` 和 `openspec validate improve-income-dashboard-motion --strict`；用合成资料在浏览器核对工作/午休、后台恢复、跨日、320px 与减少动态，记录通过范围和仍待真实用户反馈的边界。
