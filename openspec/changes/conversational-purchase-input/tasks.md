# Tasks

## 1. 同步批准范围

- [ ] 1.1 按七项已确认选择更新 PRODUCT_STRATEGY.md、ROADMAP.md、ARCHITECTURE.md、PRD-0001 与 ADR-0006/0007 及索引，明确新旧范围和实施状态；核对不再将三字段仅填空白与对话改口同时写为现行要求，并保留历史实验事实。

## 2. 扩展本机识别契约

- [ ] 2.1 扩展 `/api/assist` 当前回答、问题标识和最少上下文请求及字段补丁响应，前后端校验白名单、大小、来源和枚举；用 scripts/test_jev_server.py 覆盖非法上下文、越界、缺密钥及同源限制，断言不转发无关字段。
- [ ] 2.2 复用并扩展候选识别，支持作息、直接月工时、固定支出及明确覆盖/付款状态，区分保留、赋值与未解决；离线验证零固定支出、小数时间、双休缺日小时、否定改口、金额与税口径独立及候选来源拒绝。
- [ ] 2.3 新增固定虚构中文多轮样本和预期，逐字段核验真实 Jev 输出，每样本每轮一次调用且关闭自动重试；记录请求数、模型、服务失败、误填和漏填，禁止把旧三字段实验当作本轮通过。发现无依据填值或歧义强行确定则暂停交付并修正；缺少运行条件时明确该任务未完成。

## 3. 共用草稿与有界追问

- [ ] 3.1 将对话、快捷选项和手动输入接入同一草稿更新路径，保存字段依据、近似状态及未确认状态；扩展 tests/assist.test.tsx 验证混合约数、统一开关、改口与入口切换不错误提升证据。
- [ ] 3.2 实现按必要缺项选择固定问题、一次澄清上限、跳过与未知停止，以及显式余量开启；以可运行测试验证不重复问已知项、跳过后可确认不足结果、余量未开启不追问及快捷按钮零模型请求。
- [ ] 3.3 在 AssistInput/App 中提供默认一句话入口、可编辑摘要和一次整体确认，保留继承草稿的手动入口；更新页面文案与样式，验证完整示例无需返回表单二次确认，直接月工时与作息均可完成流程。
- [ ] 3.4 集中处理所有输入变化的结果与导出失效，清空旧决定及理由并提示重新选择；回归验证“确认—选决定—对话或手动改值—再确认—导出”不会携带旧决定，未重新选择时导出决定为 null。
- [ ] 3.5 将请求版本绑定当前草稿和问题，编辑、入口切换、取消及清空时丢弃旧响应；用延迟响应检查覆盖竞争场景，失败时保留回答和草稿并能手动继续，不自动重试。

## 4. 集成验证与交付说明

- [ ] 4.1 更新 README 与 docs/design/purchase-page-copy.md 的对话入口、主动发送范围、服务失败和本机运行说明；检查全仓现行文案无绝对不上传、仅发送单段描述或重复确认承诺，历史资料保持历史标注。
- [ ] 4.2 执行 `bun run check` 和 `uv run --locked python -m unittest discover -s scripts -p 'test_jev_server.py'`，确认既有计算和导出回归通过，记录真实失败及剩余限制。
- [ ] 4.3 完成键盘与窄屏真实页面验证：完整一句话、缺项追问、跳过、改口、余量、服务失败转手动和清空；检查开页/手动/快捷按钮不调用 Jev、请求不携带无关内容、对话原文不入导出，记录输入和确认次数与同任务手动基线，不将模拟页面或接口成功当成真实识别证明。
- [ ] 4.4 执行 `openspec validate conversational-purchase-input --strict` 与 `git diff --check`，对照场景汇总已通过和未验证项；不以任务文件齐全宣称功能交付，不自动归档或发布。
