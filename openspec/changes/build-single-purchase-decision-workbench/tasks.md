本任务清单只有在本 OpenSpec change 已获 Alune 明确批准、且 `PRD-0001` 的 `Implementation Authorized` 门禁已通过后才能执行。当前清单只规划本地形成性研究原型的实现与验证，不授权初始化、安装依赖、编写实现、部署或参与者分发；明确排除 `FR-20`、`FR-21`、`FR-26`、`AC-18`、`AC-19`、`AC-27`、研究存储 A/B、正式同意/撤回、研究观察、招募、研究日历、云托管、公开远程访问和任何正式产品能力。所有实现前运行证据必须保持“待实施”，不得把任务勾选或 OpenSpec 批准写成 `Prototype Accepted`、`Recruitment Authorized` 或研究授权。

## 1. 授权、范围与合成证据基线

- [ ] 1.1 在开始任何实现任务前核对 Alune 对本 OpenSpec 的批准、PRD `Implementation Authorized` 状态、三个 ADR 的 `Accepted` 状态和当前 Git commit；若任一门禁不成立，保持只读并不创建框架、依赖、产物或服务。[追踪：capability=`local-research-delivery/交付物必须是可复现的纯静态研究构建`；PRD=`§8.1 Implementation Authorized`、`AC-15`；ADR=`ADR-0001 §1/§5.3`、`ADR-0002 §9.2`、`ADR-0003 §9.2`；VAL=`VAL-01/VAL-12`]
- [ ] 1.2 建立本变更的范围守卫和越界检查：仅允许当前会话内存、产品外人工复盘交接和用户主动本地 JSON/Markdown 导出；对源码、fixture、导出字段、日志和构建产物检查不得出现研究编号、联系人、同意/撤回、观察、招募、研究存储、云服务或正式产品对象。[追踪：capability=`purchase-decision-workbench/研究说明、隐私告知与随时退出`、`session-snapshot-export/Privacy boundary and user-controlled local output`、`local-research-delivery/浏览器运行时必须保持纯客户端且无产品数据服务`；PRD=`FR-20/FR-21/FR-26`、`AC-18/AC-19/AC-27`（明确排除）、`AC-13–AC-15`、`AC-22–AC-25`；ADR=`ADR-0001 §6.3`、`ADR-0003 §6.2–§6.3`；VAL=`VAL-08/VAL-12`]
- [ ] 1.3 建立只含合成数据的 fixture 与证据目录，覆盖 `SYN-01`–`SYN-05`、空/零/负数、超限、minor unit、周期/币种/税口径冲突、正负 tie、负余量、恶意 Markdown/Unicode、最大修订和故障场景；为每个 fixture 标注预期精确值、状态、原因、导出语义和不含个人数据的证据输出。[追踪：capability=`purchase-decision-calculation/合成 fixture 与边界结果合同`、`session-snapshot-export/JSON and Markdown preserve one semantic snapshot`、`local-research-delivery/合成会话必须满足固定实验室性能预算`；PRD=`AC-05–AC-11`、`AC-20–AC-24`、`AC-26`；ADR=`ADR-0002 ADR-C-07`、`ADR-0003 ADR-E-09`、`ADR-0001 §9.2`；VAL=`VAL-01–VAL-10`]

## 2. 精确工具链、静态构建与本地交付基础

- [ ] 2.1 在授权门禁通过后创建精确的 package manifest 和 `package-lock.json`：Node.js `24.19.0`/npm `11.17.0`，React/React DOM `19.2.8`，以及 ADR-0001 白名单中的 TypeScript、Vite、插件、类型声明、Vitest、jsdom、Testing Library、Playwright 和 axe 版本；运行时直接依赖只保留 React/React DOM，禁止范围版本、浮动标签和白名单外直接依赖。[追踪：capability=`local-research-delivery/工具链和直接依赖必须精确锁定并可重建`；PRD=`AC-15`、`AC-25`、`AC-26`；ADR=`ADR-0001 §5.1.1`；VAL=`VAL-01/VAL-12`]
- [ ] 2.2 创建最小 React/TypeScript/Vite 静态构建配置：严格类型检查、production `dist`、`sourcemap=false`、无 SSR/API/Functions/PWA/Service Worker/数据库、无远程资源，并验证构建输入只来自批准 commit、锁文件、配置和合成 fixture。[追踪：capability=`local-research-delivery/交付物必须是可复现的纯静态研究构建`；PRD=`AC-15`、`AC-25`；ADR=`ADR-0001 §5.1`、`§5.3`、`§8.2`；VAL=`VAL-01/VAL-12`]
- [ ] 2.3 配置并验证锁定版本的 `vite preview` 本地交付：默认 `127.0.0.1:4173`、`strictPort`、无自动换端口；以 `preview.headers` 提供 `Cache-Control: no-store`、`X-Content-Type-Options: nosniff`、`Referrer-Policy: no-referrer` 及规定 CSP；跨设备验收只允许显式核验的研究者控制 LAN 地址，禁止 `0.0.0.0`、公共/访客网络、公开 URL、云托管和长期服务；若不能证明头部、缓存或日志边界则 fail-closed。[追踪：capability=`local-research-delivery/本地交付必须默认 loopback 且受控 LAN 只能显式启用`、`静态响应必须提供安全头并禁止缓存`、`访问日志和终端输出必须不持久化且不含敏感值`；PRD=`AC-01`、`AC-15`、`AC-25`；ADR=`ADR-0001 §4.1`、`§5.1.2`、`§6.3`、`§8.3`；VAL=`VAL-02/VAL-03/VAL-08`]
- [ ] 2.4 实现非循环的构建元数据与 artifact manifest：`app_version` 来自根 `package.json` 精确 SemVer（首版 `0.1.0`），`config_version` 为 `research-static-config@1.0.0`，Git SHA 为 40 位小写值；按规范 POSIX 路径和 UTF-8 字节顺序记录 `dist`、lockfile、配置、ruleset、货币快照及 schema/format 常量，manifest 自身不进入条目，`dist/index.html` 的摘要 carrier 先以 64 个 ASCII `0` 规范化后再计算并写回真实 SHA-256。[追踪：capability=`local-research-delivery/每个研究构建必须带有可核验标识和摘要`、`session-snapshot-export/Versioned JSON wire contract with explicit semantics`；PRD=`AC-01`、`AC-15`、`AC-25`、`AC-26`；ADR=`ADR-0001 §5.1.2`、`§8.2`、`ADR-0003 §5.1.2`；VAL=`VAL-01/VAL-11/VAL-12`]
- [ ] 2.5 在干净环境完成供应链 readback：用 `npm ci` 复现 lockfile，审计直接/传递依赖、许可证、安装脚本、来源完整性、peer/engine 和安全通告；任一白名单外依赖、未说明脚本、关键风险、漂移或摘要不一致都阻止构建。[追踪：capability=`local-research-delivery/供应链必须在安装前后完成可审计的 fail-closed 检查`；PRD=`AC-15`、`AC-25`、`AC-26`；ADR=`ADR-0001 §5.1.1`、`§7`；VAL=`VAL-01/VAL-12`]

## 3. 纯 TypeScript 规则内核与会话合同

- [ ] 3.1 定义框架无关的领域类型和显式上下文：金额 minor units、正分母有理数、周期 `{kind, customLabel, revision}`、币种快照、税口径、完整时间上下文、输入/结果可用性、证据状态、依赖、原因、假设、限制和版本；规则内核不得依赖 React、DOM、网络、文件、locale、console、随机数、存储或隐式系统时钟。[追踪：capability=`purchase-decision-calculation/纯内存、无 I/O 与跨浏览器确定性`、`比较周期、币种、税口径与时间上下文`；PRD=`AC-05–AC-11`、`AC-20`、`AC-24`；ADR=`ADR-0002 §5.1.2`、`§5.1.3`、`§6.3`；VAL=`VAL-01/VAL-03/VAL-09/VAL-11`]
- [ ] 3.2 实现输入解析与 fail-closed 限额：trim/符号识别顺序、64 UTF-16 code units 原始上限、固定 ASCII 十进制正则、金额/工时 token 与整数/小数上限、零/负数规则、每次约分后 128 位有理数上限及全部稳定原因码；禁止 locale 清洗、截断、填零、浮点和旧值回退。[追踪：capability=`purchase-decision-calculation/受限 ASCII 数值输入与边界`、`稳定原因码与非敏感失败合同`；PRD=`AC-05`、`AC-07`、`AC-08`、`AC-20`、`AC-21`、`AC-24`；ADR=`ADR-0002 §5.1.1`、`§5.1.5`；VAL=`VAL-01/VAL-04/VAL-05`]
- [ ] 3.3 将批准的 SIX XML 货币快照作为构建期只读 fixture：固定 `currencyTableSnapshotId`、来源/发布日期/读取日/bytes/SHA-256，去重并校验大写三字母代码和 `0/2/3/4` minor unit；`N.A.`、缺失、冲突、未知和运行时远程查询必须 fail-closed，禁止 FX。[追踪：capability=`purchase-decision-calculation/比较周期、币种、税口径与时间上下文`、`规则与货币快照版本不可变`；PRD=`AC-05`、`AC-06`、`AC-20`、`AC-21`、`AC-24`；ADR=`ADR-0002 §4.5`、`§5.1.2`、`§5.1.7`；VAL=`VAL-03/VAL-09/VAL-12`]
- [ ] 3.4 实现 BigInt 定点、有理数约分、正负 `half-away-from-zero` 舍入和最终展示合同：Income Rate 为 `min(4,max(minorUnit,2))` 位，Work-time Equivalent 为 2 位，精确非零小时不能显示为零；展示值永远不回流下游公式。[追踪：capability=`purchase-decision-calculation/BigInt 定点与有理数精确性`、`展示舍入、负值和不可回流`；PRD=`AC-06`、`AC-07`、`AC-09`、`AC-20`、`AC-21`、`AC-24`；ADR=`ADR-0002 ADR-C-03`、`§5.1.4`、`§5.1.6`；VAL=`VAL-01/VAL-02/VAL-05/VAL-07`]
- [ ] 3.5 实现五项公式、依赖图和局部可用性：精确计算 `income-rate`、`work-time-equivalent`、`coverage-available-margin`、`purchase-after-margin`、`purchase-impact`；保留可安全结果，税前/覆盖不足/购买周期未确认只停止其依赖项，禁止跨期、换汇、默认工时、clamp 或财务建议。[追踪：capability=`purchase-decision-calculation/五项公式的依赖与局部可用性`；PRD=`AC-07–AC-10`、`AC-20`、`AC-21`、`AC-24`；ADR=`ADR-0002 §5.1.4`、`§5.1.5`；VAL=`VAL-01/VAL-05/VAL-06/VAL-07`]
- [ ] 3.6 实现证据状态、原因优先级、结果 envelope 和版本追踪：只使用产品财务允许的四类状态，隔离系统/研究 `actual`，返回完整公式/单位/币种/周期/时间/依赖/原因/舍入/ruleset/currency 快照元数据，意外异常统一为 `rule-execution-failed`。[追踪：capability=`purchase-decision-calculation/证据状态优先级与状态隔离`、`稳定原因码与非敏感失败合同`、`规则与货币快照版本不可变`；PRD=`AC-06–AC-11`、`AC-20–AC-24`；ADR=`ADR-0002 ADR-C-05`、`ADR-C-08`、`§5.1.5`、`§5.1.7`；VAL=`VAL-01/VAL-05/VAL-08/VAL-12`]
- [ ] 3.7 实现浏览器时间适配器而非内核时钟读取：将设备时钟规范为严格可往返的 `Date.prototype.toISOString()` UTC instant，分别保存 `occurredAtUtc`/`recordedAtUtc`，处理 IANA 时区、offset 和 `Etc/UTC` fallback；时区只作为元数据，不改变公式结果。[追踪：capability=`purchase-decision-calculation/比较周期、币种、税口径与时间上下文`；PRD=`AC-06`、`AC-11`、`AC-21`、`AC-22`；ADR=`ADR-0002 §5.1.3`、`§5.1.7`、`ADR-0003 §5.1.2`；VAL=`VAL-01/VAL-08/VAL-09`]
- [ ] 3.8 实现确认修改、取消、失效和修订事件：编辑立即进入 `pending-reconfirmation`，旧结果不参与当前计算；取消不产生事件、不恢复旧结果；真实变更只生成一条最小 `confirmed-input-revision`，最多 50 条、每条最多 5 个旧结果，第 51 条在确认前阻止，刷新/关闭清除全部内存历史。[追踪：capability=`purchase-decision-calculation/已确认输入的修改、取消与重新确认`；PRD=`AC-11`、`AC-14`、`AC-22`；ADR=`ADR-0002 ADR-C-05`、`§5.1.7`、`ADR-0003 ADR-E-02`；VAL=`VAL-04/VAL-08/VAL-11`]
- [ ] 3.9 用 `SYN-01`–`SYN-05` 和边界 fixture 完成规则内核表驱动测试及跨浏览器规范向量：核对精确数学值、状态、primary reason、时间/版本元数据和确定性，确保不使用 Number、隐式时钟、网络、存储或旧结果。[追踪：capability=`purchase-decision-calculation/合成 fixture 与边界结果合同`；PRD=`AC-05–AC-11`、`AC-20`、`AC-21`、`AC-24`；ADR=`ADR-0002 ADR-C-07`；VAL=`VAL-01–VAL-10`]

## 4. 会话工作台与可访问交互

- [ ] 4.1 实现内存 session reducer/state machine 和单向模块依赖：区分 draft、confirmed、derived、`pending-reconfirmation`、stale、决定/复盘和导出资格；严格分离 `periodRef.revision`、`sessionRevision`/`snapshot_revision` 与 `confirmed-input-revision.sequence` 三个命名空间；UI 不计算公式、不直接调用 serializer、不访问网络/存储。[追踪：capability=`purchase-decision-workbench/修改后的失效与重新确认`、`当前会话内存与用户控制的数据生命周期`；PRD=`AC-11`、`AC-13`、`AC-14`、`AC-22`、`AC-23`；ADR=`ADR-0001 §1`、`ADR-0002 §5.1.7`、`ADR-0003 ADR-E-02`；VAL=`VAL-02/VAL-04/VAL-08/VAL-11`]
- [ ] 4.2 实现四阶段流程、首次研究/隐私说明、当前任务和退出路径：输入与期待 → 确认口径与状态 → 理解与推演 → 决定与复盘交接；说明只承诺内存处理和主动导出例外，不暗示正式同意/研究授权，退出不要求解释且不自动保存。[追踪：capability=`purchase-decision-workbench/四阶段购买决策会话`、`研究说明、隐私告知与随时退出`；PRD=`AC-01`、`AC-02`、`AC-05`、`AC-10`、`AC-12–AC-17`；ADR=`ADR-0001 §3`、`§6.2–§6.3`、`ADR-0003 §6.2`；VAL=`VAL-02/VAL-04/VAL-06/VAL-08`]
- [ ] 4.3 实现最小输入表单与确认交互：比较周期、收入、税口径、工作小时、币种、购买价格、购买周期归属、价值期待，以及可选固定成本/覆盖状态/说明；区分 user-confirmed 与 estimated，不要求账单、链接、精确日期、跨期或跨币种转换。[追踪：capability=`purchase-decision-workbench/最小输入、证据标记与用户确认`；PRD=`AC-02–AC-06`、`AC-20`、`AC-24`；ADR=`ADR-0002 ADR-C-02`、`§5.1.1–§5.1.3`；VAL=`VAL-04/VAL-05/VAL-06`]
- [ ] 4.4 实现结果卡片、证据状态、查看依据和局部数据不足文案：只呈现规则内核返回的输入、来源、单位、币种、周期、税口径、公式、时间、假设、限制、原因和恢复动作；不复算、不显示旧值、不把估算/预测变成事实或建议。[追踪：capability=`purchase-decision-workbench/结果的局部安全降级`、`依据、状态与不确定性展示`；PRD=`AC-06–AC-10`、`AC-20`、`AC-21`、`AC-24`；ADR=`ADR-0002 ADR-C-05`、`ADR-C-06`；VAL=`VAL-01/VAL-04/VAL-05/VAL-07`]
- [ ] 4.5 实现五种用户决定、依据/待确认条件和可选复盘条件或日期：允许“购买/等待/调整条件/不购买/暂不决定”且不评价、不排序、不推荐；复盘明确为产品外人工交接，不创建提醒、联系、上传或研究记录。[追踪：capability=`purchase-decision-workbench/五种用户决定与产品外复盘条件`、`低刺激、非评判的用户可见文案`；PRD=`AC-12`、`AC-13`、`AC-17`；ADR=`ADR-0001 §3`、`ADR-0002 §2/§6.2`；VAL=`VAL-04/VAL-06/VAL-08`]
- [ ] 4.6 完成响应式、键盘、焦点、ARIA、桌面/移动 VoiceOver 和 `prefers-reduced-motion` 实现：四阶段、依据、修改、决定、复盘、导出入口和退出均可操作，状态/错误/不确定性不只靠颜色、动画、声音或位置表达。[追踪：capability=`purchase-decision-workbench/响应式、键盘与 VoiceOver 可完成性`、`local-research-delivery/非必要动态必须减少并尊重用户的减少动态偏好`；PRD=`AC-16`、`AC-17`、`AC-23`；ADR=`ADR-0001 §3`、`§5.1.1`、`§9.1`；VAL=`VAL-06/VAL-10`]

## 5. ExportSnapshotV1 与双格式导出

- [ ] 5.1 实现导出资格、冻结和 stale 生命周期：至少一个稳定、已提交且 `availability != not-provided` 的 user-confirmed/estimated 输入；冻结开始/结束 `sessionRevision` 必须相同并复制为 `snapshot_revision`，不得与 `periodRef.revision` 或事件 `sequence` 混用；预览不得创建 Blob/object URL；预览后的确认、重算、周期/币种/时区/构建变化必须阻止旧快照。[追踪：capability=`session-snapshot-export/Export eligibility and immutable snapshot lifecycle`；PRD=`AC-11`、`AC-22`、`AC-23`；ADR=`ADR-0003 ADR-E-02`、`§5.1.1`；VAL=`VAL-01/VAL-02/VAL-03`]
- [ ] 5.2 实现快照级 Unicode scalar 预检和规范化：CRLF/CR→LF；NUL 或孤立 surrogate 使 JSON/Markdown 同时失败；建立封闭字段和值类型、固定输入/结果集合、10 个字典数组、可用性/证据状态/来源/原因代码和 0–50 条修订的内部 structural validator；逐项校验 `comparison_context`、结果、修订 `before/after`、`old_results` 的封闭字段，以及全部 ID 数组的固定顺序、去重和逐项对应关系。[追踪：capability=`session-snapshot-export/Versioned JSON wire contract with explicit semantics`、`Separate availability, evidence, source, and value kinds`；PRD=`AC-06`、`AC-21`、`AC-22`、`AC-24`；ADR=`ADR-0003 ADR-E-03`、`ADR-E-09`、`§5.1.1–§5.1.3`；VAL=`VAL-01/VAL-02/VAL-04/VAL-05`]
- [ ] 5.3 实现 JSON serializer：固定 snake_case 顶层字段及顺序、11 个输入、5 个结果、唯一键、UTF-8/无 BOM/LF/单个结尾 LF、`application/json` 且不附加 `charset`、精确十进制或分子/分母字符串、构建/ruleset/货币元数据和显式 null/空数组；通过结构断言和 JSON round-trip 后才允许继续。[追踪：capability=`session-snapshot-export/Versioned JSON wire contract with explicit semantics`；PRD=`AC-06`、`AC-21`、`AC-22`、`AC-24`；ADR=`ADR-0003 ADR-E-03`；VAL=`VAL-01/VAL-02/VAL-05`]
- [ ] 5.4 实现安全 GFM Markdown serializer 和可逆解码器：固定章节/表格/段落/代码块顺序；用户文本隔离到至少三个且长度大于内容连续同类标记的字面围栏；禁止 HTML、脚本、图片、链接、autolink、远程资源；按 `unicode-visible-escape-v1` 编码并保持逐 scalar 可逆。[追踪：capability=`session-snapshot-export/Safe GFM Markdown is a reversible projection`；PRD=`AC-17`、`AC-22`、`AC-23`；ADR=`ADR-0003 ADR-E-04`、`§5.1.4`；VAL=`VAL-04/VAL-05`]
- [ ] 5.5 实现 JSON/Markdown 跨格式等价断言：两种格式只允许 `format_name`、`format_version`、`file_generated_at` 和文件名表示差异，共用 `snapshot_revision`/`snapshot_captured_at`，按稳定 field ID 比较输入、结果、状态、依赖、原因、决定、复盘和修订；覆盖 `SYN-02`、`SYN-04` 及恶意文本 fixture。[追踪：capability=`session-snapshot-export/JSON and Markdown preserve one semantic snapshot`；PRD=`AC-11`、`AC-21`、`AC-22`；ADR=`ADR-0003 ADR-E-02`、`ADR-E-09`；VAL=`VAL-02/VAL-04/VAL-05`]
- [ ] 5.6 实现逐格式预览/确认状态与文件名时间合同：每种格式独立 `idle/previewing/ready/generating/download-requested/failed/cancelled`；文件名使用冻结时 UTC 的共同 ASCII stem `zhiguan-purchase-decision-YYYYMMDDTHHMMSSZ`，`file_generated_at` 单独记录；取消不生成任何临时对象，导出可跳过且不影响 Decide/退出。[追踪：capability=`session-snapshot-export/Preview, explicit confirmation, and per-format download request state`；PRD=`AC-22`、`AC-23`；ADR=`ADR-0003 ADR-E-05`、`§5.1.1`、`§5.1.5`；VAL=`VAL-03/VAL-06/VAL-10`]
- [ ] 5.7 实现 DOM download adapter、稳定错误和幂等清理：明确用户激活内同步创建单格式 Blob/object URL/临时 anchor 并 `click()`；正常返回只设 `download-requested`；下一 macrotask 或 `pagehide` 清理，失败不自动重试/分享/剪贴板/网络兜底，JSON 与 Markdown 可部分失败且清理失败阻止新请求。[追踪：capability=`session-snapshot-export/Stable errors, partial failure, and temporary resource cleanup`；PRD=`AC-23`；ADR=`ADR-0003 ADR-E-05`、`ADR-E-07`、`§5.1.5–§5.1.6`；VAL=`VAL-06/VAL-07/VAL-08`]
- [ ] 5.8 实现资源上限、兼容拒绝和导出契约暂停：按规范化 UTF-8 bytes enforce 50 修订、5 旧结果、64 KiB 文本、512 KiB 快照、1 MiB/格式和单字符串/Blob/URL；超限不截断/压缩/拆包/重试；拒绝未知 major/重复键/缺字段，不实现 importer、签名、加密容器或长期归档；任何 export-contract breach 暂停并触发整体回滚。[追踪：capability=`session-snapshot-export/Fixed resource limits and fail-closed generation`、`Version compatibility and whole-build rollback boundary`；PRD=`AC-22`、`AC-23`、`AC-26`；ADR=`ADR-0003 ADR-E-07`、`ADR-E-08`、`ADR-E-09`、`§8.2–§8.3`；VAL=`VAL-01/VAL-06/VAL-09/VAL-11`]

## 6. 集成、隐私与浏览器验证

- [ ] 6.1 完成纯内核、session reducer、workbench view model 和导出领域层的单元/集成测试：覆盖确认/取消/失效、局部不足、证据传播、决定/复盘、冻结/stale、结构 validator、JSON round-trip、Markdown 解码和跨格式等价；测试输出不得含个人数据或敏感值。[追踪：capability=`purchase-decision-calculation`、`purchase-decision-workbench`、`session-snapshot-export` 全部相关 requirements；PRD=`AC-05–AC-14`、`AC-20–AC-24`；ADR=`ADR-0002 ADR-C-03/C-05/C-07`、`ADR-0003 ADR-E-02/E-03/E-04/E-09`；VAL=`VAL-01/VAL-02/VAL-04/VAL-05/VAL-08`]
- [ ] 6.2 用 Playwright 完成合成主流程/失败流程和浏览器边界验证：捕获仅有静态资源请求，检查 CSP/响应头、Cookie/Web Storage/IndexedDB/Cache Storage/URL/剪贴板/console、下载请求和页面生命周期；确认无产品数据、研究数据、遥测、远程依赖或持久日志。[追踪：capability=`local-research-delivery/静态资源之外必须零网络请求且不得写浏览器持久状态`、`访问日志和终端输出必须不持久化且不含敏感值`、`session-snapshot-export/Privacy boundary and user-controlled local output`；PRD=`AC-14`、`AC-15`、`AC-22`、`AC-23`、`AC-25`；ADR=`ADR-0001 §5.1.2`、`§6.3`、`§8.3`、`ADR-0003 §6.3`；VAL=`VAL-02/VAL-03/VAL-05/VAL-08`]
- [ ] 6.3 在记录实际版本的 macOS/Windows Chrome、macOS Safari 和 iOS Safari 上完成桌面键盘、移动/窄屏、macOS/iOS VoiceOver、焦点、状态、错误、敏感提示和减少动态人工验收；axe 只作为补充，不得替代人工结论。[追踪：capability=`purchase-decision-workbench/响应式、键盘与 VoiceOver 可完成性`、`local-research-delivery/目标浏览器、键盘和 VoiceOver 路径必须可完成`；PRD=`AC-16`、`AC-17`、`AC-23`；ADR=`ADR-0001 §3`、`§5.1.1`、`§9.1`；VAL=`VAL-06/VAL-10`]
- [ ] 6.4 在记录的目标设备、网络和最大合成会话数据上测量首个可用界面、输入重算、导出预览和单格式生成，分别满足 `3s`、`100ms`、`500ms`、`1s`；性能证据只含合成数据，不产生遥测或真实用户事件。[追踪：capability=`local-research-delivery/合成会话必须满足固定实验室性能预算`、`session-snapshot-export/Fixed resource limits and fail-closed generation`；PRD=`AC-26`；ADR=`ADR-0001 §9.1`、`ADR-0003 ADR-E-07`；VAL=`VAL-07/VAL-09`]
- [ ] 6.5 完成可复现构建 readback：在受信干净环境运行 `npm ci`、`tsc --noEmit`、测试和 Vite production build，扫描 `dist`/manifest/依赖/静态外联/source map/真实或研究数据，核对 Node/npm、commit、配置、ruleset、货币快照、schema/format 和摘要；任一不一致阻止启动或导出。[追踪：capability=`local-research-delivery/交付物必须是可复现的纯静态研究构建`、`每个研究构建必须带有可核验标识和摘要`、`供应链必须在安装前后完成可审计的 fail-closed 检查`；PRD=`AC-01`、`AC-15`、`AC-25`、`AC-26`；ADR=`ADR-0001 §5.1.1–§5.1.2`、`§7`、`§8.2`；VAL=`VAL-01/VAL-12`]
- [ ] 6.6 执行停止、首次撤下和整体回滚演练：隐私/外发/日志/导出/状态/性能/无障碍/供应链失败时先停止分发、停服务、关端口、清理应用可控副本；首次构建直接撤下，后续恢复同一完整 Git commit、dist、manifest、lockfile、Node/npm、配置、ruleset、货币、schema/format 单元，并重新执行受影响 readback；明确已下载文件不可召回。[追踪：capability=`local-research-delivery/回滚必须恢复完整的已验证研究交付单元`、`会话结束必须清除运行状态并留下安全退出证据`、`session-snapshot-export/Version compatibility and whole-build rollback boundary`；PRD=`AC-14`、`AC-15`、`AC-23`、`AC-25`、`AC-26`；ADR=`ADR-0001 §7`、`§8.3`、`ADR-0003 §8.3`；VAL=`VAL-08/VAL-11`]
- [ ] 6.7 汇总 AC/VAL 证据矩阵并保持治理状态准确：链接 `AC-01–AC-17`、`AC-20–AC-26` 与 `VAL-01–VAL-12` 的实际证据，明确所有结果仍为实施后待验收；不勾选或宣称 `Prototype Accepted`，不触发 `RESEARCH-0001` Accepted、`Recruitment Authorized`、参与者分发或正式产品发布。[追踪：capability=`local-research-delivery/回滚必须恢复完整的已验证研究交付单元`、`purchase-decision-workbench/当前会话内存与用户控制的数据生命周期`；PRD=`§8.1–§8.3`、`AC-01–AC-17`、`AC-20–AC-26`（研究治理 AC-18/19/27 排除）；ADR=`ADR-0001 §9.2`、`ADR-0002 §9.2`、`ADR-0003 §9.2`；VAL=`VAL-01–VAL-12`]
