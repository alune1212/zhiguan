# Tasks

## 1. 同步批准范围

- [x] 1.1 按七项已确认选择更新 PRODUCT_STRATEGY.md、ROADMAP.md、ARCHITECTURE.md、PRD-0001 与 ADR-0006/0007 及索引，明确新旧范围和实施状态；核对不再将三字段仅填空白与对话改口同时写为现行要求，并保留历史实验事实。

## 2. 扩展本机识别契约

- [x] 2.1 扩展 `/api/assist` 当前回答、问题标识和最少上下文请求及字段补丁响应，前后端校验白名单、大小、来源和枚举；用 scripts/test_jev_server.py 覆盖非法上下文、越界、缺密钥及同源限制，断言不转发无关字段。
- [x] 2.2 复用并扩展候选识别，支持作息、直接月工时、固定支出及明确覆盖/付款状态，区分保留、赋值与未解决；离线验证零固定支出、小数时间、双休缺日小时、否定改口、金额与税口径独立及候选来源拒绝。
- [x] 2.3 新增固定虚构中文多轮样本和预期，逐字段核验真实 Jev 输出，每样本每轮一次调用且关闭自动重试；记录请求数、模型、服务失败、误填和漏填，禁止把旧三字段实验当作本轮通过。发现无依据填值或歧义强行确定则暂停交付并修正；缺少运行条件时明确该任务未完成。

## 3. 共用草稿与有界追问

- [x] 3.1 将对话、快捷选项和手动输入接入同一草稿更新路径，保存字段依据、近似状态及未确认状态；扩展 tests/assist.test.tsx 验证混合约数、统一开关、改口与入口切换不错误提升证据。
- [x] 3.2 实现按必要缺项选择固定问题、一次澄清上限、跳过与未知停止，以及显式余量开启；以可运行测试验证不重复问已知项、跳过后可确认不足结果、余量未开启不追问及快捷按钮零模型请求。
- [x] 3.3 在 AssistInput/App 中提供默认一句话入口、可编辑摘要和一次整体确认，保留继承草稿的手动入口；更新页面文案与样式，验证完整示例无需返回表单二次确认，直接月工时与作息均可完成流程。
- [x] 3.4 集中处理所有输入变化的结果与导出失效，清空旧决定及理由并提示重新选择；回归验证“确认—选决定—对话或手动改值—再确认—导出”不会携带旧决定，未重新选择时导出决定为 null。
- [x] 3.5 将请求版本绑定当前草稿和问题，编辑、入口切换、取消及清空时丢弃旧响应；用延迟响应检查覆盖竞争场景，失败时保留回答和草稿并能手动继续，不自动重试。

## 4. 集成验证与交付说明

- [x] 4.1 更新 README 与 docs/design/purchase-page-copy.md 的对话入口、主动发送范围、服务失败和本机运行说明；检查全仓现行文案无绝对不上传、仅发送单段描述或重复确认承诺，历史资料保持历史标注。
- [x] 4.2 执行 `bun run check` 和 `uv run --locked python -m unittest discover -s scripts -p 'test_jev_server.py'`，确认既有计算和导出回归通过，记录真实失败及剩余限制。
- [x] 4.3 完成键盘与窄屏真实页面验证：完整一句话、缺项追问、跳过、改口、余量、服务失败转手动和清空；检查开页/手动/快捷按钮不调用 Jev、请求不携带无关内容、对话原文不入导出，记录输入和确认次数与同任务手动基线，不将模拟页面或接口成功当成真实识别证明。
- [x] 4.4 执行 `openspec validate conversational-purchase-input --strict` 与 `git diff --check`，对照场景汇总已通过和未验证项；不以任务文件齐全宣称功能交付，不自动归档或发布。

## 本轮验证记录（2026-09-22）

- `bun run check`：依赖检查、类型检查、42 个测试及生产构建通过；Python 后端单测 20/20 通过；OpenSpec strict 与 `git diff --check` 通过。
- 真实 Jev 使用 `jev-1.13.0`：四批共 17 次虚构样本请求，零服务失败、零自动重试。首批 11 轮有 2 处差异（一处含糊改口被赋值、一处有效原文片段比预期短），回归批 3 轮有 1 处固定支出短答漏填。已修复含糊改口及短答规则，修复后使用不同虚构样本验证；未重跑失败样本。独立验证结果见忽略目录 `tmp/jev-conversation-{first,followup,regression,fixed-short}.jsonl`。这些小样本不代表一般准确率。
- 真实浏览器另发送 1 次完整虚构描述：税后月收入 12000、价格 2400、直接月工时 160，字段核对正确，结果为 32.00 小时、每小时 75.00 元。完整核心流程为一句输入、一次整理发送、一次整体确认；手动作息基线为 4 次字段填写/选择及一次确认。不以交互次数推断耗时收益。
- 页面模拟响应检查：缺项、未知、跳过、明确改口、一次歧义澄清、余量零支出、失败转手动、旧响应隔离和清空均通过。开页、手动与快捷按钮零 Jev 请求；追问请求只带相关字段；原文不入导出；修改后旧决定与理由为 null。键盘确认焦点到达结果标题；390px 窄屏无控件溢出，最终页面检查无运行时异常。服务失败和竞争场景用受控响应验证，不冒充真实模型验证。
- 本轮只完成本机实现和验证；未提交、发布或归档，主规格的其余 delta 同步留待归档流程。

## 归档记录（2026-09-22）

用户明确要求归档；已同步主规格并完成核验，保留 1 项非阻断交互偏差，详见 [verification.md](verification.md)。上一节“未归档”描述为实施交付时状态；本次未创建 Git 提交或发布。
