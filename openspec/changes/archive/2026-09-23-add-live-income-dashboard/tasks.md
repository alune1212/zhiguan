# Tasks

## 1. 对齐已确认的产品与数据边界

- [x] 1.1 将 RFC-0001 的已确认修订落实到 PRODUCT.md，保留 0.1.0 历史并记录 0.2.0；同步策略、路线图和指标，验证收入理解是独立任务且没有新增账户/上报要求，尚未实现的能力仍明确标注。
- [x] 1.2 对齐 PRD-0003、ADR-0008 与本设计的日历口径、主动单次覆盖、@2 快照和备份边界，标记旧 ADR 的精确替代范围；检查 ARCHITECTURE、PRD-0001、术语和数据原则不存在同一新流程的纯内存/旧公式冲突，保留旧文件含义及历史验证。

## 2. 日历与精确金额计算

- [x] 2.1 在 `src/domain/income.ts` 定义资料、时段和特殊日期校验，复用现有金额校验；用 Vitest 验证默认 5 天/8 小时、非法收入、重叠/零长度/跨日区间和无效时区拒绝，不新增重复小时字段。
- [x] 2.2 实现保存时区的当月工作区间与秒数计算，包括月首前一日跨入区间；用固定时刻测试午休/周末、日期覆盖、跨日/月、设备时区变化、夏令时缺失/歧义降级及无工作时间。
- [x] 2.3 复用 BigInt/有理数实现今日、本月、每秒速率，不先舍入工时；验证 8000 元/20 个 8 小时工作日样例、月末总额相等、小额速率、重新打开和时钟变化重算。
- [x] 2.4 扩展 `calculation.ts` 的显式日历工作时间依据，将总秒数直接接入购买计算；验证首页/购买时薪相同、既有直接工时与主动平均作息覆盖可用，估算和余量状态保持正确。

## 3. 快照、本地存储与备份

- [x] 3.1 将 `App.tsx` 中导出逻辑按需提取并定义 @2 快照，记录月份、时区、basis 模式、规则版本和精确结果；验证五项结果及决定字段保留、单次覆盖不冒充日历、旧 @1 文件不被改写。
- [x] 3.2 实现 `local-data.ts` 的单文档校验、序列化和受保护读写，使用原生锁与 revision 检测；测试配额/权限/锁不可用、损坏/未知版本、多标签冲突和写入失败均不覆盖已有资料且不声称成功。
- [x] 3.3 实现主动收藏及单项删除，快照与基础资料独立；测试无决定/部分不足的收藏、修改资料后收藏不变、重新试算形成草稿、删除失败保留原值。
- [x] 3.4 实现 `zhiguan-local-backup@1` 导出、完整校验、覆盖确认及一次性恢复；测试精确数据往返、损坏/未知版本/@1 单次购买文件拒绝、取消/失败保持原数据，不调用网络或执行文件内容。
- [x] 3.5 实现清空资料和恢复后的状态重置；验证清空确认取消无变化、成功后重开进入首填、晚到对话响应不能恢复被清空内容，下载文件不被宣称已删除。

## 4. 首填、看板与详细设置

- [x] 4.1 在 `App.tsx` 接入只填到手月收入的首次视图与恢复路径，默认依据和本地保存告知可见；更新静态渲染测试验证无购买/固定开销/对话必填项，保存失败进入明确临时模式。
- [x] 4.2 实现今日主数字、本月/每秒速率及工作状态，定时刷新与前台恢复按 now 重算；验证休息时不增长、跨月更换分母、更新时间与展开依据可见，不逐 tick 写入存储。
- [x] 4.3 实现折叠的作息、日期覆盖、时区和收入设置，保存前提示重算；验证工作小时自动求和、修改后当前看板变化且收藏不变、非法输入保留待修正而非覆盖资料。
- [x] 4.4 完成窄屏、键盘焦点和减少动态样式；验证动态数字不抢焦点或逐秒触发读屏播报，所有设置/状态可不用颜色和动画理解。

## 5. 购买入口与对话资料复用

- [x] 5.1 将购买分析置于次级入口，预填当前到手收入和日历时间，价格可直接填写，对话按需打开；验证临时改动不反写资料、显式工时覆盖有口径说明、返回看板资料不变。
- [x] 5.2 更新 `assist-flow.ts` / `AssistInput.tsx` 的已知资料与来源处理；扩展现有测试验证不重复问已知收入/工时、默认 8 小时不伪装为模型推断、约数状态不提升、无法映射日历的作息不猜星期。
- [x] 5.3 保留余量按需、税前降级、五种决定和改口清除规则，隔离清空草稿与清空资料；验证跨月比较更新需重算确认、过期响应不覆盖草稿，收藏维持旧月份。
- [x] 5.4 在结果页接入收藏、@2 下载和收藏回看；验证完整资料/收藏/备份不进入 Jev 上下文，纯看板/手动/存储流程无外部识别请求，原服务失败可手动接续。

## 6. 集成验证与交付文档

- [x] 6.1 执行 `bun run check`，并在协议受影响时运行现有 Python 离线测试；记录准确结果及失败，不以静态渲染或规划检查替代浏览器验收，不自动发起真实模型实验。
- [x] 6.2 用合成资料在真实浏览器完成首填→动态展示→刷新/重开→修改资料→购买收藏→备份恢复→删除流程，覆盖窄屏/键盘/减少动态、两个标签保存冲突和写入失败；核对页面与快照金额及网络请求，记录未验证边界。
- [x] 6.3 同步 README、设计文案、架构和 PRD 的实际交付状态，核对新旧导出/备份的用途及回滚出口；运行文档链接检查、`git diff --check` 与 OpenSpec 严格验证，未进行的用户体验反馈或回滚演练保持待验证。
