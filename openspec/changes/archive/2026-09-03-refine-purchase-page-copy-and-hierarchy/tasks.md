## 1. 确认文案基线

- [x] 1.1 将 `docs/design/purchase-page-copy.md` 标记为已确认，逐项核对输入、结果、缺失数据和决定文案与本变更 spec 一致，并以文档状态和差异检查验证未改动产品语义

## 2. 同步页面文案与结果表达

- [x] 2.1 在 `src/app/App.tsx` 替换顶部、输入、校验和隐私文案，移除重复眉题，并以应用静态渲染测试验证三个基本数字、可选固定支出、确认按钮和隐私说明均使用已确认文字
- [x] 2.2 在 `src/app/App.tsx` 用最小纯展示函数生成工作时间、每小时收入和余量句子，只读取领域结果的精确十进制字符串，并以 Vitest 覆盖工作时间、正余量、零余量和负余量且确认导出仍保留原始负值
- [x] 2.3 将估算、预测和不可用状态改为具体句子，覆盖缺失、格式错误、税前收入和固定支出未确认分支，并以测试验证核心结果不会被可选余量问题阻塞、税前提示要求重新填写到手收入且页面不显示零值或旧值
- [x] 2.4 将五种决定显示文字、原因说明和下载按钮同步为已确认文案，并以测试验证五个既有决定代码及 `zhiguan-purchase-decision@1` 导出字段完全不变

## 3. 同步信息层级与可访问结构

- [x] 3.1 将结果 DOM 顺序调整为工作时间优先、其他结果随后，移除独立状态徽章和状态说明块，并以渲染断言验证主要结论先于补充结果且结果标题继续接收提交后的焦点
- [x] 3.2 在 `src/styles/app.css` 仅用现有 CSS 强化主要结果、弱化补充结果并清理废弃状态样式，以桌面和窄屏页面检查验证无横向滚动、长文案正常换行、焦点和错误样式仍清晰可见
- [x] 3.3 核对标签、错误关联、展开说明和决定控件的程序化名称与阅读顺序，并以纯键盘和 VoiceOver 检查验证状态不依赖颜色、所有操作可到达且工作时间结论最先被读到
  - 2026-09-03：用户确认「VoiceOver 通过」；实际读屏验收由用户完成，非自动化替代。

## 4. 完整验证

- [x] 4.1 更新 `tests/app.smoke.test.tsx` 的文案与展示分支断言，同时保持 `tests/calculation.test.ts` 的计算合同不变，并运行 `npm run check` 验证类型、全部测试和生产构建通过
- [x] 4.2 在本地页面分别检查基本输入、含大概数、可选余量、税前收入、缺失输入、正零负余量和五种决定路径，并记录实际页面与 `docs/design/purchase-page-copy.md` 无偏差
- [x] 4.3 检查最终差异只涉及确认文档、`App.tsx`、`app.css` 和相关应用测试，运行 `git diff --check` 并确认没有新增依赖、网络、持久化、输入项或导出格式变化
