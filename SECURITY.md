# 安全与隐私

## 当前边界

当前实现是 React 单页应用：会话只在浏览器内存中处理，不使用运行时 API、数据库、账户、Cookie、浏览器持久化、分析或外部网络。用户只有在明确操作后才会生成本地 JSON；下载后的文件由用户设备控制。

不要把真实收入、资产、身份、联系人、密钥、未脱敏日志或截图放入仓库、公开报告或公开讨论。

## 报告问题

可能涉及漏洞、越权、数据泄露或敏感复现细节时，请通过 GitHub 的私密安全入口报告：

<https://github.com/alune1212/zhiguan/security/advisories/new>

公开渠道只描述不含敏感细节的影响和复现条件。无法使用私密入口时，只留下“需要私密安全联系渠道”，不要补充细节。

## 开发规则

- 输入校验失败、数据缺失或冲突必须明确降级，不能用默认值掩盖。
- 修改数据/隐私边界、接入外部服务、增加持久化/同步或改变公开导出格式前，先写短 ADR。
- 完成功能后运行相关测试、类型检查或构建，并说明未验证的边界。
