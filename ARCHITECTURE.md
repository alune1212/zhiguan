# 值观架构约束

> 这是产品宪法在系统边界上的约束文件。它规定必须守住的性质，不规定具体框架、云服务、数据库或部署方式。

| 项目 | 内容 |
| --- | --- |
| 状态 | 治理基线；研究原型的三项 ADR 均为 `Accepted`；[`build-single-purchase-decision-workbench`](openspec/changes/build-single-purchase-decision-workbench/proposal.md) 已于 `2026-08-24` 获 Alune 批准，`Implementation Authorized` 已通过；只允许按批准任务实施，不代表 `Prototype Accepted`、`Recruitment Authorized` 或参与者分发获准 |
| 版本 | 0.1.0 |
| 生效日期 | 2026-08-20 |
| 负责人 | Alune |
| 复审周期 | 每半年一次，或在重大数据/信任边界变化时 |
| 上位文档 | [`PRODUCT.md`](PRODUCT.md) |

## 1. 目的与边界

值观要把时间、收入、成本、目标、订阅和物品使用转换为可理解、可追溯的长期价值。架构必须让用户知道数据从哪里来、经过什么转换、结论有多确定，并且在需要时可以带走自己的数据。

本文件只固定以下长期约束：

- 隐私与数据主权；
- 事实、估算、预测和数据不足的明确区分；
- 派生结果的可解释性与可追溯性；
- 历史口径、单位和计算规则的版本化；
- 数据的可迁移、可导出与可删除；
- 身份、权限、秘密、日志和外部连接的安全边界；
- 未来技术决策必须经过 ADR 的门禁。

本文件不选定编程语言、前端/后端框架、数据库、云厂商、消息系统、身份提供商或具体部署拓扑。任何这类选择都必须在需求明确后通过 ADR 记录依据、替代方案、迁移和回滚路径。

产品决策优先级为：`PRODUCT.md > PRODUCT_STRATEGY.md > ROADMAP.md > PRD/RFC > Issue > 实现`。本文件服从 `PRODUCT.md`，并以跨功能的长期系统约束规范 PRD、RFC、ADR、Issue 与实现。批准后的 ADR 在其适用技术范围内优先于 Issue 和实现，但不能覆盖本文件或任何上位产品文档。发生冲突时，先暂停实现并提交 RFC/ADR，不能用实现细节绕过上位约束。

## 2. 架构原则

### 2.1 数据主权优先

- 只收集完成明确用户任务所需的最少数据；每类数据都要说明用途、保存期限和删除方式。
- 用户主动提供的数据属于用户。导入、同步、分析和分享必须有明确的授权边界，不能因为功能方便而默认扩大范围。
- 任何可导出或可删除的承诺都必须在产品设计阶段写入 PRD，并在验收中验证，而不是留作以后补充。
- 不以个人数据换取与当前任务无关的广告画像、排名或增长指标。

### 2.2 可解释而不装懂

- 任何重要金额、时长、进度、成本、收益或建议，都应能回溯到输入、单位、时间窗口、计算规则、假设和数据状态。
- 结果应区分“计算得到的事实”“用户确认的事实”“估算”“预测”和“数据不足”；不得把不确定结果渲染成确定结论。
- 用户需要时能看到简明解释和详细依据。解释应服务于选择，不替用户定义什么值得。
- AI 或自动化只可在有证据、可引用、可拒绝的边界内工作；没有依据时必须返回数据不足或要求用户确认。

### 2.3 数据状态不可静默升级

每个影响决策的数值、记录和派生结果，都必须带有数据状态。状态不是展示标签，而是影响计算、排序、建议和验收的约束。

| 状态 | 含义 | 允许的表达 | 不允许的表达 |
| --- | --- | --- | --- |
| `actual` 实际 | 来自可识别来源的原始记录，或由系统记录的实际事件 | 显示来源、时间和原始值 | 不得隐藏来源或把缺失字段补成实际值 |
| `user-confirmed` 用户确认 | 用户明确确认的输入或修正值 | 显示确认时间、确认者和变更 | 不得把默认值或未保存编辑视为确认 |
| `estimated` 估算 | 由输入和明确假设推得的近似值 | 显示方法、假设和误差/范围 | 不得混入实际统计而不标记 |
| `forecast` 预测 | 对未来的推演或情景结果 | 显示时间范围、模型/规则和不确定性 | 不得承诺结果或冒充已发生事实 |
| `insufficient-data` 数据不足 | 当前证据不足以可靠计算或比较 | 解释缺什么、如何补齐 | 不得静默使用零值、旧值或猜测填充 |

状态变化必须可追溯：分别记录当前证据状态、原始状态、来源链、触发者、时间、依据和前后值。用户确认是独立的确认元数据，不能把 `estimated` 或 `forecast` 自动升级为 `actual`；若输入变化触发重算，应创建带新状态、规则版本和生成时间的新结果，并保留旧结果的历史快照。

派生结果默认采用以下保守传播顺序；未来 ADR 可以细化字段结构，但不能降低不确定性：

1. 缺少任一关键输入时，结果为 `insufficient-data`；
2. 结果描述未来情景时，结果为 `forecast`；
3. 任一关键输入为 `estimated` 时，非未来结果为 `estimated`；
4. 任一关键输入仅由用户确认、其余输入均为 `actual` 时，结果为 `user-confirmed`；
5. 只有全部关键输入均为 `actual`、结果也描述已发生事实时，结果才可为 `actual`。

用户可以确认对估算或预测的理解与采用，但该确认不能改变派生结果的 `estimated` 或 `forecast` 状态。

### 2.4 历史口径必须可重现

- 每条历史记录保留原始输入、原始单位、发生时间、记录时间、来源和修订历史。
- 时间、货币、时区、周期、工作日/自然日等口径必须显式保存，不能依赖当前默认设置解释过去。
- 计算规则、分类规则和指标定义需要有可识别版本。规则变化不得静默改写用户已经看到的历史结论。
- 需要追溯时，系统应能回答“当时看到的是什么”和“按当前规则重算会是什么”；两者必须明确区分。
- 删除请求、纠错和合并操作只保留完成安全、合规或故障追踪所必需的最小审计元数据。删除后不得保留原始敏感内容、可逆摘要或可重新关联用户的标识；允许保留的事件类型、字段、去关联方式、期限、备份清除窗口和验证方法必须在 PRD/ADR 中明确。没有可说明的必要性时，审计事件也应删除。

### 2.5 可迁移与可导出

- 任何核心数据都应有稳定的机器可读导出，以及用户可读的摘要或报告；格式和版本在导出物中自描述。
- 导出应覆盖原始记录、确认/修订历史、数据状态、来源、单位、规则版本和用户创建的标签/目标，不能只导出最终数字。
- 导入必须提供预览、字段映射、冲突处理和失败报告，不得静默覆盖原数据。
- 内部模型可以演进，但不能以不可逆的内部格式锁定用户。弃用字段或格式前，必须给出迁移工具/说明和过渡期限。
- 导入、导出、备份和分享的权限边界不同，不能把“能导出”默认为“能公开分享”。

### 2.6 安全边界清晰

- 将个人财务、收入、账户标识、工作记录、消费和设备标识视为敏感数据；默认最小权限和最小暴露。
- 身份认证、授权、会话、密钥/秘密、数据存储、传输、备份、日志和外部集成必须分别定义边界，不得由一个宽泛的“可信服务”概念代替。
- 秘密、访问令牌和原始敏感内容不得进入源码、提交记录、错误消息、分析事件、普通日志或截图。
- 管理操作、导入导出、分享、批量删除、规则切换和外部同步应有可审计的授权与结果反馈。
- 失败应安全降级：宁可显示数据不足或停止同步，也不能用过期、猜测或越权数据生成确定性结论。
- 第三方服务只获得完成任务所需的最小字段和最短期限；其数据保留、撤销和故障影响必须在 PRD/RFC 中说明。

### 2.7 低输入负担与可撤回

- 任何需要长期维护的输入都必须说明频率、预期耗时、缺失时的行为和停止/删除方式。
- 自动化可以降低重复输入，但必须让用户知道自动化做了什么，并提供纠正、暂停和撤回路径。
- 不以连续打卡、通知轰炸或羞耻化分数制造依赖；数据暂时不完整时，产品应帮助用户恢复，而不是惩罚用户。

## 3. 数据生命周期与派生结果契约

一个数据对象从进入系统到离开系统，应经过可识别的生命周期：

```text
采集/导入 → 校验与归一化 → 用户确认（如需要）
       → 计算/推演 → 展示与解释 → 修订/撤回 → 导出或删除
```

每一阶段至少要回答：

1. 谁或什么产生了它？
2. 它的来源、发生时间、记录时间、单位和数据状态是什么？
3. 它是否被修改、归一化、合并或推演？规则版本是什么？
4. 用户如何查看依据、纠正、撤回、导出或删除？
5. 失败、冲突、缺失或外部服务不可用时，系统如何安全降级？

影响用户决定的派生结果，至少要携带以下语义（具体字段名由未来 ADR 决定）：

| 语义 | 最低要求 |
| --- | --- |
| 输入集合 | 可识别输入及其版本/时间范围 |
| 计算口径 | 公式或规则说明、货币/时间/单位口径 |
| 数据状态 | 结果及关键输入的状态 |
| 生成时间 | 何时计算、适用到何时 |
| 不确定性 | 假设、范围、置信说明或数据不足原因 |
| 可操作性 | 如何补数据、纠正、重算、撤回或删除 |

展示层可以把复杂信息压缩成一句话，但不能丢失回到详细依据的路径。

## 4. 不可默认改变的架构契约

以下内容属于长期信任契约，不能仅因一次迭代、指标压力或技术便利而改变：

- 用户数据的最小收集、用途限制、可导出、可删除和撤回权；
- 数据状态的五类区分及其不确定性表达；
- 核心派生结果必须有输入、口径、版本和解释路径；
- 历史结论不能在未说明的情况下被新规则静默改写；
- 失败时不可用猜测或过期数据伪装成确定结果；
- 安全边界、最小权限和秘密不进日志/源码的要求。

以下内容可以演进，但必须保留兼容与解释路径：

- 数据模型内部组织和索引方式；
- 计算实现和缓存策略；
- 导入/导出适配器与展示形式；
- 交互、通知和默认设置；
- 具体存储、部署、认证和观测技术。

若变化会影响上述不可默认改变的契约，必须先提交并批准 RFC；只有 RFC 明确授权时，才能修订 `PRODUCT.md` 或本文件的对应原则，并通过 ADR 记录具体技术影响、迁移和回滚。

## 5. 未来 ADR 门禁

在开始实现或采购以下能力前，必须有被批准的 ADR；“先做一个临时版本”不绕过门禁：

- 持久化模型、数据分层、同步策略或离线策略；
- 身份认证、授权模型、组织/个人边界和会话管理；
- 加密、密钥管理、备份、恢复、删除和数据保留策略；
- 时间、货币、周期、舍入和历史重算的实现口径；
- 计算/规则引擎、预测模型、AI 或外部模型调用；
- 导入、导出、公开分享和第三方连接的格式与权限；
- 日志、分析、监控、错误上报和个人数据脱敏；
- 任何会破坏历史可重现性、迁移能力或用户控制权的变更。

ADR 至少需要说明：背景与决策驱动因素、候选方案、选择及理由、对产品与数据信任的影响、兼容/迁移/回滚、验证证据和后续复审条件。ADR 的工作流与模板见 [`docs/adr/README.md`](docs/adr/README.md) 和 [`docs/adr/template.md`](docs/adr/template.md)。当前已批准 [`ADR-0001`](docs/adr/ADR-0001-delivery-form-and-technology-stack.md) 的研究原型交付形态与技术栈、[`ADR-0002`](docs/adr/ADR-0002-calculation-and-rules.md) 的计算与规则合同，以及 [`ADR-0003`](docs/adr/ADR-0003-export-contract.md) 的导出合同。[`build-single-purchase-decision-workbench`](openspec/changes/build-single-purchase-decision-workbench/proposal.md) 的 proposal、specs、design 与 tasks 已于 `2026-08-24` 获 Alune 明确批准，`PRD-0001` 的 `Implementation Authorized` 同日通过；后续只能按批准任务实施，运行证据仍须由 `Prototype Accepted` 门禁验收。

## 6. 架构变更检查表

提交 PRD、RFC、ADR 或实现前，作者应确认：

- [ ] 目标用户任务和价值循环阶段已写清；
- [ ] 每类输入/输出标明来源、用途、保存期限与数据状态；
- [ ] 事实、估算、预测和数据不足没有混用；
- [ ] 结果可回溯到输入、口径、规则版本和生成时间；
- [ ] 隐私、权限、秘密、日志和第三方边界已评估；
- [ ] 导出、删除、纠错、失败降级和长期维护负担有明确行为；
- [ ] 是否需要 RFC/ADR 已判断；需要时已链接批准记录；
- [ ] 测试或验收能够证明上述约束，而非只证明页面能显示。

### 待未来决策的问题

以下问题仍刻意留给后续 RFC/ADR，不由已批准的原型 ADR 外推：

- 身份、同步和多设备边界如何落地；
- 正式产品的核心数据存储、备份和跨会话恢复如何实现；
- 正式产品的运行时、托管、观测和成本约束如何组合；
- 超出 `ADR-0002` 固定公式的预测模型、AI 或外部规则如何治理。
