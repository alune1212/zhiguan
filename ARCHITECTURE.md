# 值观架构约束

这份文档只保留当前实现必须守住的边界。产品原则以 PRODUCT.md 为准。

## 当前实现

- React + TypeScript + Vite 单页应用；Bun 统一管理依赖并运行开发、测试和构建工具，应用本身在浏览器中运行。工具版本与配置以 `package.json`、`bunfig.toml` 和 `bun.lock` 为准，执行命令见 `README.md`。
- 会话状态只存在浏览器内存；刷新、关闭或退出后不恢复。
- 不使用运行时 API、数据库、账户、云端同步、Cookie、Web Storage、IndexedDB 或分析上报。
- 计算在浏览器内完成；当前实现只支持 CNY 月度口径，金额使用整数分精确计算。
- 当前工作台先展示工作时间结论，以每小时收入作为补充；当前固定支出字段非空，或支出覆盖、购买月份确认任一有值时，再展示本月可用金额和购买后余量。单独填写价值期待不触发余量结果。导出继续保留购买影响，它只是购买前后余量的差异，不重复计算固定支出。固定支出未填写、未确认完整覆盖或收入为税前口径时只让余量结果降级，不阻塞核心结果；只有购买后余量和购买影响依赖购买归属本月的确认，两者可计算时均为 forecast。
- 购买决策由用户明确触发本地 JSON 导出；文件进入设备后由用户控制，应用不上传任何内容。
- 当前会话没有确认与修改历史，已有决定会在输入重新确认后保留；追溯缺口见 `docs/product/prd/PRD-0001-single-purchase-decision-workbench.md`，不能将当前一次性导出视为完整历史记录。

## 必须保持

- 输入不完整、冲突或无效时局部降级为 insufficient-data，不填零、旧值或猜测。
- 保留 actual、user-confirmed、estimated、forecast 和 insufficient-data 的产品语义；当前输入只产生 user-confirmed 或 estimated，不将自填数字标为 actual。可计算的未来结果使用 forecast，无法计算时使用 insufficient-data；每个结果能回到输入、口径、公式、假设和限制。界面用单一“这些数字里有大概数”选择替代四个数字状态下拉；开启时将四个数字字段标为 estimated，关闭时标为 user-confirmed，点击“确认并查看结果”即确认本次输入，导出仍按每个数字字段保留 evidence 结构。
- 用户自己决定购买、等待、调整、不购买或暂不决定；产品不评分、不排名、不催促。
- Decide 保留可选的价值期待、五种决定和用户依据；当前不采集复盘条件或日期，也不提供提醒或研究记录。
- 键盘、窄屏、辅助技术和减少动态路径仍可完成核心流程。
- 不新增持久化、网络、身份、外部服务或公开格式。若确需改变不可逆数据/隐私边界、接入外部服务、持久化/同步或修改公开格式，先写一个短 ADR。

## 当前架构决策

- ADR-0001：本地浏览器内存态 React/Vite 单页交付。
- ADR-0002：CNY 精确分、确定性计算、证据状态和数据不足降级。
- ADR-0003：当前购买决策快照的一次性 JSON 导出。

除上述不可逆边界外，优先用现有代码和浏览器能力完成用户功能；不为未来可能的账户、同步、研究或 AI 预留框架。
