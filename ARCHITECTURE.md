# 值观架构约束

产品原则以 PRODUCT.md 0.2.0 为准。本文件记录当前代码结构、收入看板实施边界及仍待完成的验收。

## 当前实现

- React + TypeScript + Vite 单页应用，入口为 `IncomeApp`，包含基础设置、收入看板、购买分析和收藏；Bun 管理依赖并运行开发、检查和构建工具。工具版本与配置以 `package.json`、`bunfig.toml` 和 `bun.lock` 为准，命令见 `README.md`。
- 收入基础资料及用户主动收藏保存在浏览器 `localStorage` 的版本化文档中；读写使用 revision 与 Web Locks 检查并发，支持手动备份、恢复、删除和清空。收入看板与本地操作不自动调用外部服务；不引入账户、云端同步或行为上报。
- 收入计算按选定时区、周作息和特殊日期计算当月计划工作秒数；今日、本月与每秒速率均为 `estimated`，不代表实际出勤、到账或资产增长。日历购买快照使用 @2 格式；旧 @1 文件保持原含义，不自动转换或导入。
- 购买草稿和对话状态只存在浏览器内存；关闭或刷新后不恢复未收藏的购买内容。用户主动使用 Jev 时，浏览器向本机回环 Python 服务发出请求；请求只带当前回答、当前问题和理解所需的最少上下文，服务端按问题白名单裁剪，不记录请求正文，密钥只由服务端 `.env` 加载。
- 购买金额按 CNY 精确分与有理数计算。收入看板进入购买时默认使用当月日历工作时间；用户可主动选择原平均作息或直接月工时作为单次覆盖，作息推导保持 `estimated`。购买工作台以工作时间等价为主要结果，每小时收入为补充；开启余量后才收集相应信息，可计算的未来余量结果为 `forecast`。
- 输入变化使购买结果和导出失效，并清除旧决定、理由和 `reviewCondition`；价值期待独立保留。用户主动下载 @2 或收藏结果；收藏保留当时输入和计算口径，不按后续资料改写。购买影响只在导出中保留，不重复计算固定支出。

## 收入看板实现边界

- 首页先提供收入动态看板。首次默认双休、工作日 09:00–12:00 与 13:00–18:00；用户填写到手月收入并主动保存后，同一浏览器可恢复基础资料。默认作息及其派生结果始终是 estimated。
- 看板按所选时区将当月收入分摊到当月计划工作区间，今日/本月累计在访问或恢复时按当前时间重算；不累计实际出勤、资产或到账，也不依赖逐秒写入持久化数据。
- 按 ADR-0008 保存在单个本地资料文档中的范围仅为用户保存的基础资料和主动收藏。导入、导出、恢复、删除及失败保护按 ADR-0008 执行；备份格式 `zhiguan-local-backup@1` 独立于购买快照。
- 从看板进入购买时，默认继承当月日历工作时间。用户可以明确选择将原有平均作息或直接月工时仅作为该次购买比较的覆盖口径；展示并快照所选依据，不改写长期资料，不静默切换口径。
- 新日历购买快照为 `zhiguan-purchase-decision@2`；旧 `zhiguan-purchase-decision@1` 继续保留原含义，不推断日历资料、不自动转换或导入。新功能不增加账户、跨设备同步或行为上报。

收入看板、本地资料、备份恢复与购买快照已接入代码，自动检查和合成数据下的浏览器主路径均已通过。真实用户体验、回滚演练及正式发布仍待验证。

## 必须保持

- 输入不完整、冲突或无效时局部降级为 insufficient-data，不填零、旧值或猜测。
- 保留 actual、user-confirmed、estimated、forecast 和 insufficient-data 的产品语义；直接填写或对话识别的输入在摘要经用户整体确认前不得视作已确认；约数依据与字段绑定，整体确认、切换入口或统一估算选项不得将其提升为精确值。由作息换算的工时始终为 estimated。可计算的未来结果使用 forecast，无法计算时使用 insufficient-data；每个结果能回到输入、口径、公式、假设和限制。导出仍按每个数字字段保留 evidence 结构，不包含对话原文。
- 用户自己决定购买、等待、调整、不购买或暂不决定；产品不评分、不排名、不催促。
- Decide 保留可选的价值期待、五种决定和用户依据；当前不采集复盘条件或日期，也不提供提醒或研究记录。
- 键盘、窄屏、辅助技术和减少动态路径仍可完成核心流程。
- 仅 ADR-0007 批准的用户主动 Jev 购买对话可调用外部模型；收入看板、基础资料、备份和收藏不得发送给 Jev。ADR-0008 已批准同浏览器本地持久化及其明确的本地格式；账户、云端/跨设备同步、自动行为上报以及超出 ADR-0008 的数据留存或公开格式变更仍不在授权范围，需新的 ADR。

## 当前架构决策

- ADR-0001：原始范围为单次购买工作台的浏览器内存态 React/Vite 交付；收入看板流程中的本地持久化按 ADR-0008 有限替代。
- ADR-0002：CNY 精确分、确定性计算、证据状态和数据不足降级。
- ADR-0003：当前购买决策快照的一次性 JSON 导出。
- ADR-0005：明确作息估算、证据状态与增量导出依据。
- ADR-0006：已被 ADR-0007 替代，保留首版三字段辅助填写及实验记录。
- ADR-0007：默认一句话对话、按需单题追问、同一草稿和一次整体确认、最少上下文、输入改动后清除旧决定、失败接续；已完成本机实现与验证。
- ADR-0008：批准的收入看板、同浏览器基础资料、主动收藏、本地备份恢复、当月日历分摊及新 `@2` 快照已接入代码并通过自动检查；合成数据下的浏览器主路径已通过。真实用户体验、回滚演练及正式发布仍待验证。它仅在收入看板与由其进入的新购买流程中替代 ADR-0001 的内存态范围、ADR-0002/0005 的默认 52/12 平均工时口径及 ADR-0003 的 `@1` 导出/不持久化限制。原规则和 @1 历史文件含义继续保留。

除上述不可逆边界外，优先用现有代码和浏览器能力完成用户功能；不为未来可能的账户、同步、研究或 AI 预留框架。ADR-0007 是当前唯一的 AI 与外部服务例外；公开部署、多人服务和其他 AI 能力不在范围内。
