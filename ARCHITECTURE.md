# 值观架构约束

这份文档只保留当前实现必须守住的边界。产品原则以 PRODUCT.md 为准。

## 当前实现

- React + TypeScript + Vite 单页应用。
- 会话状态只存在浏览器内存；刷新、关闭或退出后不恢复。
- 不使用运行时 API、数据库、账户、云端同步、Cookie、Web Storage、IndexedDB 或分析上报。
- 计算在浏览器内完成；当前实现只支持 CNY 月度口径，金额使用整数分精确计算。
- 当前工作台输出收入速率、工作时间等价、覆盖范围内可用余量、购买后余量和购买影响；购买影响只是购买前后余量的差异，不重复计算固定支出。余量依赖固定支出覆盖确认，购买路径依赖购买归属当前周期确认，购买后余量与购买影响均为 forecast。
- 独立 Goal 区域只计算一个用户定义的数字目标：完成比例与不小于零的剩余差距；它不进入购买计算，也不增加历史或预测。
- 购买决策和 Goal 各自由用户明确触发本地 JSON 导出；文件进入设备后由用户控制，应用不上传任何内容。

## 必须保持

- 输入不完整、冲突或无效时局部降级为 insufficient-data，不填零、旧值或猜测。
- 结果区分 actual、user-confirmed、estimated、forecast 和 insufficient-data；每个结果能回到输入、口径、公式、假设和限制。
- 用户自己决定购买、等待、调整、不购买或暂不决定；产品不评分、不排名、不催促。
- Decide 保留价值期待、五种决定和用户依据；Review 只保留用户填写的复盘条件或日期，不变成提醒或研究记录。
- 键盘、窄屏、辅助技术和减少动态路径仍可完成核心流程。
- 不新增持久化、网络、身份、外部服务或公开格式。若确需改变不可逆数据/隐私边界、接入外部服务、持久化/同步或修改公开格式，先写一个短 ADR。

## 当前架构决策

- ADR-0001：本地浏览器内存态 React/Vite 单页交付。
- ADR-0002：CNY 精确分、确定性计算、证据状态和数据不足降级。
- ADR-0003：当前购买决策与 Goal 快照的一次性 JSON 导出。

除上述不可逆边界外，优先用现有代码和浏览器能力完成用户功能；不为未来可能的账户、同步、研究或 AI 预留框架。
