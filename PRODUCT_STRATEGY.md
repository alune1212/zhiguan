# 值观产品策略

**状态：** 当前工作假设，服从 PRODUCT.md。

## 当前问题

用户在考虑一项购买时，往往需要把收入、工作时间、固定成本、价格和自己的期待放在一起理解，但不想先维护完整账本。当前只验证这一购买闭环。独立 Goal 进度曾作为相邻最小切片，已于 2026-09-01 因用户确认增加输入负担且未帮助购买任务而撤下，保留在 PRD-0002 作为历史记录；这不是研究或市场结论。

## Now 的产品目标

让用户在一个轻量会话中：

1. 用一句话提供本次购买所需信息；对话入口整理已提供内容，代码只追问核心结果确实缺少的信息。用户也可随时切到手动填写；需要时再开启余量输入；
2. 先看购买金额对应的工作时间，以每小时收入作为补充；只有用户主动开启余量后，才补充并显示本月可用金额和购买后余量；
3. 看懂每个结果的含义、公式、数据状态和不足原因；作息换算明确标为 estimated，不要求用户先自行统计月工时；
4. 自己选择购买、等待、调整条件、不购买或暂不决定；
5. 在需要时主动导出当前 JSON。

对话输入改造已获批准，正在实施。用户可用一句话整理月收入、税口径、购买价格、平时作息或直接月工时；只有用户主动发送时才调用本机 Python 服务与 TypeSafe/Jev。首次发送前说明处理范围；每次请求限于当前回答、当前问题及理解所需的少量相关字段或原文片段，不带完整表单、全部聊天记录、价值期待或决定理由。代码决定是否追问，并提供固定问题和少量快捷项；快捷项不调用 Jev。用户可编辑同一份待确认摘要，整体确认一次后看结果。明确改口可更新旧值，结果变化后清除旧决定及理由。服务失败保留进度，可主动重试或继续手动填写。会话只在内存中，原文不进入导出。

## 优先顺序

1. 直接改善上述用户流程和结果理解；
2. 保持输入校验、证据状态、可解释性、可访问性和隐私边界；余量只在用户主动开启后渐进收集，缺少完整覆盖或只提供税前收入时只降级余量；购买后余量还需确认按本月付款；
3. 用真实使用反馈决定是否扩大范围；
4. 仅在不可逆边界或外部接入前补充必要 ADR。

## 暂不做

独立 Goal 进度（已于 2026-09-01 撤下）、完整记账、投资/财富建议、账户和同步、云端分享、提醒、AI 建议、自由文本生成、研究招募、研究存储和自动化数据收集都暂停。当前获准的 AI 能力限于本次单次购买对话输入：Jev 做有界语义识别，代码控制追问与计算；不扩展为通用 AI 入口或自由文本生成，其他能力不是当前开发的隐含承诺。

## 如何判断下一步

当前不设未经验证的增长目标。只观察用户能否用少量输入完成“理解 → 推演 → 决定”，能否指出来源和不确定性，以及对话主入口和手动备用入口是否降低而不是增加输入负担。反馈只用于当前产品判断，不声称研究或市场结论；证据不足时保持 insufficient-data，不因完成一次原型就扩大产品承诺。
